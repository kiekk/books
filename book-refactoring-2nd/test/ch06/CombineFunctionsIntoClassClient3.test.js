import {expect} from 'chai';
import {basicChargeAmount} from '../../src/ch06/CombineFunctionsIntoClass-client3'

describe('CombineFunctionsIntoClass-Client3.js', () => {
    it('basicChargeAmount', () => {
        expect(basicChargeAmount).to.equal(1);
    });

});
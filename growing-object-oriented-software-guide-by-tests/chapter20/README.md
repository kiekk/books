## 20장 테스트에 귀 기울이기

### 20.1 대체할 수 없는 객체에 대해 목 객체를 적용해야 한다(마법을 쓰지 않고)

#### 20.1.1 싱글턴은 의존성이다
#### 20.1.2 절차에서 객체로
#### 20.1.3 암시적인 의존성도 여전히 의존성이다

```markdown
전역 값을 사용해 캡슐화를 우회함으로써 컴포넌트 호출자로부터 의존성을 감출 수 있지만 그렇게 한다고 해서 의존성이 사라지지는 않으며, 단지 의존성에 접근할 수 없게 될 뿐이다.
```
